/** @type {import('next').NextConfig} */
const nextConfig = {}

// next.config.js
// tailwind.config.js

module.exports = {
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
      },
    },
  },
  variants: {},
  plugins: [],
};
